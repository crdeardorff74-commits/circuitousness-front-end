# Local-only files. These repos auto-deploy, so anything committed goes live.

# Internal working docs — deliberately NOT published (user call 2026-09-05).
# Note this stops FUTURE exposure only; earlier commits still hold them.
CLAUDE.md
NOTES.md

# Claude Code per-project settings
.claude/

# Windows shell artifact
nul

# The deploy zip is built by the user's BAT file from these same files.
*.zip

# Local scratch art; nothing shipped references it.
images/
